#-*- mode: org -*-

* Scoping: disallow duplicate variables in equation blocks
  e.g., 1 where { x = 2; x = 2; } should be rejected.
* Allow contravariant subtyping in patterns
* Implement a rudimentary module system
* Rename Coercions into Coercion
